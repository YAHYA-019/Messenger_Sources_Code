package X;

import android.graphics.Bitmap;
import java.io.File;
import java.io.OutputStream;

/* loaded from: GHs.class */
public interface GHs {
    boolean AH4(Bitmap bitmap, File file);

    boolean AH5(Bitmap bitmap, OutputStream outputStream);
}
