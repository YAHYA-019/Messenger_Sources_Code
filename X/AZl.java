package X;

import android.content.Context;
import com.facebook.messaging.model.threadkey.ThreadKey;
import com.facebook.ui.emoji.model.Emoji;

/* loaded from: AZl.class */
public interface AZl {
    void Cni(Context context, ThreadKey threadKey, Emoji emoji, Long l);

    void Cnk(Context context, ThreadKey threadKey, Emoji emoji);
}
