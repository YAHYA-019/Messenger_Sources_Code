package com.facebook.messaging.montage.composer.art.circularpicker;

import X.11T;
import android.content.Context;
import android.util.AttributeSet;
import android.widget.ImageView;
import com.facebook.widget.CustomFrameLayout;

/* loaded from: IconEmptyView.class */
public final class IconEmptyView extends CustomFrameLayout {
    public ImageView A00;

    /* JADX WARN: 'super' call moved to the top of the method (can break code semantics) */
    public IconEmptyView(Context context) {
        super(context, null);
        11T.A0F(context, 1);
    }

    /* JADX WARN: 'super' call moved to the top of the method (can break code semantics) */
    public IconEmptyView(Context context, AttributeSet attributeSet) {
        super(context, attributeSet);
        11T.A0F(context, 1);
    }

    /* JADX WARN: 'super' call moved to the top of the method (can break code semantics) */
    public IconEmptyView(Context context, AttributeSet attributeSet, int i) {
        super(context, attributeSet);
        11T.A0F(context, 1);
    }
}
